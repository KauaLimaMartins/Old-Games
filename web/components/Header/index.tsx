import React from 'react';

import { Container } from './styles';

const Header: React.FC = () => {
    return (
        <Container>
            <img src="/logo.png" alt="old games logo" />
        </Container>
    );
};

export default Header;
